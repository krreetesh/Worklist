# Uncomment the next line to define a global platform for your project

project 'Worklist.xcodeproj'
platform :ios, '9.3'
use_frameworks!

target 'Worklist' do
  inhibit_all_warnings!
  pod 'JJFloatingActionButton'
  pod 'iCarousel'
end
